var commandLine = args();
var bindHost = "0.0.0.0";
var bindPort = 4040;
var historyLimit = 50;
var roomPassword = NULL;

if (len(commandLine) > 0)
    bindHost = commandLine[0];
if (len(commandLine) > 1)
    bindPort = Int(commandLine[1]);
if (len(commandLine) > 2)
    historyLimit = Int(commandLine[2]);
if (len(commandLine) > 3)
    roomPassword = commandLine[3];
if (historyLimit < 1)
    historyLimit = 1;

var clients = Array{};
var names = Array{};
var history = Array{};

fun findClient(handle) {
    var index = 0;
    while (index < len(clients)) {
        if (clients[index] == handle)
            return index;
        index++;
    }
    return -1;
}

fun nameTaken(candidate) {
    var index = 0;
    while (index < len(names)) {
        if (names[index] == candidate)
            return true;
        index++;
    }
    return false;
}

fun sendLine(handle, text) {
    var result = netSecureSendLine(handle, text, 2000);
    return result[0] == "sent";
}

fun removeClientAt(index, announce) {
    var handle = clients[index];
    var name = names[index];

    netSecureForget(handle);
    netClose(handle);
    removeAt(clients, index);
    removeAt(names, index);

    if (announce && name != NULL) {
        var notice = "* " + name + " left";
        print(notice, "\n");
        flush();
        broadcast(notice);
    }
}

fun broadcast(text) {
    var index = 0;
    while (index < len(clients)) {
        if (sendLine(clients[index], text)) {
            index++;
        } else {
            removeClientAt(index, false);
        }
    }
}

fun appendHistory(text) {
    push(history, text);
    while (len(history) > historyLimit)
        removeAt(history, 0);
}

fun sendHistory(handle) {
    if (!sendLine(handle, "HISTORY " + String(len(history))))
        return false;

    for (var message in history) {
        if (!sendLine(handle, message))
            return false;
    }

    return sendLine(handle, "END");
}

fun registerName(handle, candidate) {
    var index = findClient(handle);
    if (index < 0)
        return false;

    if (len(candidate) == 0)
        return sendLine(handle, "ERROR name cannot be empty");

    if (len(candidate) > 96)
        return sendLine(handle, "ERROR name is too long");

    if (candidate[0] == '/')
        return sendLine(handle, "ERROR name cannot start with '/'");

    if (nameTaken(candidate))
        return sendLine(handle, "ERROR name is already in use");

    names[index] = candidate;

    if (!sendLine(handle, "OK " + candidate)) {
        removeClientAt(index, false);
        return false;
    }

    if (!sendHistory(handle)) {
        index = findClient(handle);
        if (index >= 0)
            removeClientAt(index, false);
        return false;
    }

    var notice = "* " + candidate + " joined";
    print(notice, "\n");
    flush();
    broadcast(notice);
    return findClient(handle) >= 0;
}

fun processLine(handle, line) {
    var index = findClient(handle);
    if (index < 0)
        return false;

    if (names[index] == NULL)
        return registerName(handle, line);

    if (line == "/quit") {
        sendLine(handle, "BYE");
        index = findClient(handle);
        if (index >= 0)
            removeClientAt(index, true);
        return false;
    }

    if (line == "/help") {
        if (!sendLine(handle, "COMMANDS /help /history /quit")) {
            index = findClient(handle);
            if (index >= 0)
                removeClientAt(index, true);
            return false;
        }
        return true;
    }

    if (line == "/history") {
        if (!sendHistory(handle)) {
            index = findClient(handle);
            if (index >= 0)
                removeClientAt(index, true);
            return false;
        }
        return true;
    }

    if (len(line) == 0)
        return true;

    if (len(line) > 4096) {
        if (!sendLine(handle, "ERROR message is too long")) {
            index = findClient(handle);
            if (index >= 0)
                removeClientAt(index, true);
            return false;
        }
        return true;
    }

    var message = names[index] + ": " + line;
    appendHistory(message);
    print(message, "\n");
    flush();
    broadcast(message);
    return findClient(handle) >= 0;
}

fun drainClient(handle) {
    while (true) {
        var packet = netSecureReceiveLine(handle);

        if (packet[0] == "wait")
            return true;

        if (packet[0] == "closed" || packet[0] == "error") {
            if (packet[0] == "error") {
                print("SECURITY rejected client packet: ", packet[1], "\n");
                flush();
            }
            var index = findClient(handle);
            if (index >= 0)
                removeClientAt(index, true);
            return false;
        }

        if (packet[0] == "data") {
            if (!processLine(handle, packet[1]))
                return false;
        }
    }
}

fun acceptPending(listener) {
    while (true) {
        var client = netAccept(listener);
        if (client == NULL)
            return NULL;

        var secure = netSecureServer(client, roomPassword, 7000);
        if (secure[0] != "ok") {
            print("SECURITY rejected connection: ", secure[1], "\n");
            flush();
            netClose(client);
        } else {
            push(clients, client);
            push(names, NULL);

            if (!sendLine(client, "NAME choose a unique name"))
                removeClientAt(len(clients) - 1, false);
        }
    }
}

fun main() {
    if (roomPassword == NULL || len(roomPassword) < 8) {
        print("ERROR: provide a room password with at least 8 bytes.\n");
        print("Usage: chat_server.chmp <host> <port> <history> <password>\n");
        flush();
        return NULL;
    }

    var listener = netListen(bindHost, bindPort, 64);
    print("LISTENING ", netPort(listener), " SECURE AES-256-GCM\n");
    flush();

    while (true) {
        var watched = Array{listener};
        for (var client in clients)
            push(watched, client);

        var ready = netPoll(watched, 100);
        for (var handle in ready) {
            if (handle == listener) {
                acceptPending(listener);
            } else {
                if (findClient(handle) >= 0)
                    drainClient(handle);
            }
        }
    }
}

main();
